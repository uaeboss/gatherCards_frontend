import "./css/Card.css"

const Card = ({ magic }) => {
  return (
    <>
    {/* {magic && 
      magic.map((cardurl) => {
        return (
          <div key={magic.id}>
              <img className="card_border_radius" src={cardurl.imageUrl} />
            </div>
        );
      })} */}
    </>
  );
};

export default Card;
